Review our discussion and produce blocks for any docs that changed. For each changed doc, write the filename on one line followed by the full updated section or new content in a markdown fenced block.

Then end with:

Tell Claude Code:

> <precise implementation instructions based on decisions made>

Only include docs that actually changed. Skip the Tell Claude Code block if no implementation work was identified.
