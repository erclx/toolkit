Write a Claude Code edit prompt using this format:

```markdown
`<relative/path/to/file>`

- `<old>` → `<new>`
```

Use relative paths from repo root. List one change per line. End with: "Do not modify any other content."
