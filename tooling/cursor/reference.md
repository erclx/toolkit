# Tooling cursor reference

## Overview

The cursor stack installs Cursor IDE infrastructure into a target project. Use when setting up a new project that uses Cursor as the primary editor.

## .gitignore

- Add `.cursor/.tmp/` to ignore build artefacts generated by `aitk gov build`.
